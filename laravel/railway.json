{
  "build": {
    "builder": "dockerfile"
  },
  "scripts": {
    "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
  },
  "env": {
    "APP_ENV": "production",
    "APP_DEBUG": "false"
  }
}
